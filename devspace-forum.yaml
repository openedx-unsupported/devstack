version: v2beta1
name: forum

# This is a list of `deployments` that DevSpace can create for this project
deployments:
  forum:
    # This deployment uses `helm` but you can also define `kubectl` deployments or kustomizations
    helm:
      # Under `values` we can define the values for this Helm chart used during `helm install/upgrade`
      # You may also use `valuesFiles` to load values from files, e.g. valuesFiles: ["values.yaml"]
      values:
        containers:
          - args:
              - bash
              - -c
              - source /edx/app/forum/ruby_env && source /edx/app/forum/devstack_forum_env && cd /edx/app/forum/cs_comments_service && bundle install && while true; do ./bin/unicorn -c config/unicorn_tcp.rb -I .; sleep 2; done
            image: edxops/forum:latest
            name: edx-devstack-forum
        service:
          ports:
            - containerPort: 4567
              port: 44567
              protocol: TCP

# This is a list of `dev` containers that are based on the containers created by your deployments
dev:
  forum:
    labelSelector:
      app.kubernetes.io/component: forum
    # Forward the following ports to be able access your application via localhost
    ports:
      - port: 44567:4567
dependencies:
  elasticsearch710:
    path: devspace-elasticsearch710.yaml
  memcached:
    path: devspace-memcached.yaml
  mongo:
    path: devspace-mongo.yaml
  opensearch12:
    path: devspace-opensearch12.yaml

# Define dependencies to other projects with a devspace.yaml
# dependencies:
#   api:
#     git: https://...  # Git-based dependencies
#     tag: v1.0.0
#   ui:
#     path: ./ui        # Path-based dependencies (for monorepos)
