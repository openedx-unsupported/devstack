sudo: required

language: python

python:
  - "3.5"

env:
  - DEVSTACK_WORKSPACE=/tmp

services:
  - docker

before_install:
  - make requirements
  - make clone

script:
  - make provision
  - make healthchecks
  - make up-marketing-detached
