apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.30.0 (HEAD)
  creationTimestamp: null
  labels:
    io.kompose.service: frontend-app-authn
  name: frontend-app-authn
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: frontend-app-authn
  strategy: {}
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert
        kompose.version: 1.30.0 (HEAD)
      creationTimestamp: null
      labels:
        io.kompose.network/devstack-default: "true"
        io.kompose.service: frontend-app-authn
    spec:
      containers:
        - args:
            - bash
            - -c
            - npm ci || exit 1; while true; do npm start; sleep 2; done
          env:
            - name: NODE_ENV
              value: development
          image: node:16
          name: edx.devstack.frontend-app-authn
          ports:
            - containerPort: 1999
              hostPort: 1999
              protocol: TCP
          resources: {}
          stdin: true
          tty: true
          workingDir: /edx/app/frontend-app-authn
      restartPolicy: Always
status: {}
